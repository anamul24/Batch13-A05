<<<<<<< HEAD
## 1. different between var,let,const
> var= Funtion-scoped, can be redeclared.
> let= Block-scoped, cannot be redeclared in same scope , can be updated
>const= Block-scoped, cannot be redeclared or updated

## 2. Spread Operator(...)
> expand iterable (array, object)into individual element 
    cont arr = [1,2];
    const newArr= [1,2,3]

## 3.Difference between map(),filter(),forEach()

>map()= return a new array after applying a function to each element
>filter()= return a new array with elements that pass a condition.
>forEach()= Executes a function for each element , no return value

## 4. Arrow function

> shorter syntax for function expressions
    const sum = (a,b)=> a+b;

## 5. Template literals

>string literals using backticks` that allow interpolation
    const name="anamul";
    console.log(`hello,${name}!`);
=======
## 1. different between var,let,const
> var= Funtion-scoped, can be redeclared.
> let= Block-scoped, cannot be redeclared in same scope , can be updated
>const= Block-scoped, cannot be redeclared or updated

## 2. Spread Operator(...)
> expand iterable (array, object)into individual element 
    cont arr = [1,2];
    const newArr= [1,2,3]

## 3.Difference between map(),filter(),forEach()

>map()= return a new array after applying a function to each element
>filter()= return a new array with elements that pass a condition.
>forEach()= Executes a function for each element , no return value

## 4. Arrow function

> shorter syntax for function expressions
    const sum = (a,b)=> a+b;

## 5. Template literals

>string literals using backticks` that allow interpolation
    const name="anamul";
    console.log(`hello,${name}!`);
>>>>>>> c7168df4a9768d062f1045cad1ffeb6e1341245b
